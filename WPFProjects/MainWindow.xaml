<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFProjects"
        mc:Ignorable="d"
        Title="County Assigner" Height="450" Width="600" FontSize="14px">

    <Grid Grid.Column="0" Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Customers with no county:" Grid.Row="0"/>
        <DataGrid x:Name="customerDataGrid" Grid.Row="1" Margin="0,0,0,10" SelectionMode="Single" SelectionChanged="CustomerDataGrid_SelectionChanged"/>

        <Button x:Name="okButton" Content="OK" IsEnabled="False" Grid.Row="2" Width="85" HorizontalAlignment="Right" Click="OK_Click" />

        <Label Content="Counties for this state:" Grid.Row="3"/>
        <DataGrid x:Name="countyDataGrid" Grid.Row="4" Margin="0,0,0,10" SelectionMode="Single" SelectionChanged="CountyDataGrid_SelectionChanged"/>

        <Button x:Name="updateButton" Content="Update" IsEnabled="False" Grid.Row="5" Width="85" HorizontalAlignment="Right" Click="Update_Click" />
    </Grid>

</Window>
